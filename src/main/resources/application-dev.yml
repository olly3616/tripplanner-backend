spring:
  config:
    activate:
      on-profile: dev

  # Secrets are injected via environment variables in the deploy environment.
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    root: info
    com.voyage: debug
